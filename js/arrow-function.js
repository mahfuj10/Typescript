"use strict";
function addNew(num1, num2) {
    return num1 + num2;
}
addNew(5, 2);
var add2 = function (num1, num2) { return num1 + num2; };
add2(3, 2);
var doMath = function (x, y) {
    return x + y;
};
